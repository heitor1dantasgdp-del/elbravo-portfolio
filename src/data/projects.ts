import { Project } from '../types';
import { SEED_PROJECTS } from './seed-projects';

/**
 * Default projects data exported for static initializers and fallback state.
 */
export const projectsData: Project[] = SEED_PROJECTS;

